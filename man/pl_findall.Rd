% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vprolog.R
\name{pl_findall}
\alias{pl_findall}
\title{Collect solution bindings (findall)}
\usage{
pl_findall(pl, template, goal)
}
\arguments{
\item{pl}{A \code{prolog_engine}.}

\item{template}{A term whose bindings to capture per solution (e.g. "X-Y").}

\item{goal}{The goal to satisfy (e.g. "member(X,[1,2,3])").}
}
\value{
Character vector of \code{writeq}'d solution terms (possibly empty).
}
\description{
Runs \code{goal}, writing one \code{template} instance per solution to a temp
file via \code{writeq/2}, then reads the lines back. Each returned string is a
canonical (\code{writeq}) term. Operators used in \code{template}/\code{goal}
must already be loaded (see \code{\link{pl_use}}).
}
